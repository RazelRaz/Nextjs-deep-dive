'use client'

const BlogError = () => {
    return (
        <div>
            <h1 style={{color:'red', fontSize:"30px"}}>There was an error in the Blog </h1>
        </div>
    );
};

export default BlogError;